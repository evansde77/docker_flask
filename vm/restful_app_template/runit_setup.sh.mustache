#!/bin/bash 

chmod -R a+rw /etc/sv/restful-app-uwsgi/log
chmod +x /etc/sv/restful-app-uwsgi/run 
chmod +x /etc/sv/restful-app-uwsgi/log/run 
chmod +x /etc/sv/restful-app-nginx/run  
chmod +x /etc/sv/restful-app-nginx/log/run 
chmod -R a+rw /etc/sv/restful-app-nginx/log

ln -s /etc/sv/restful-app-uwsgi /etc/service
ln -s /etc/sv/restful-app-nginx /etc/service
